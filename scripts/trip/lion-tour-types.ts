/**
 * @deprecated Import from @family-memories/planner-schema instead.
 * Thin re-export for scripts/trip CLI backward compatibility.
 */
export type {
  AgencyId,
  DayPlan,
  FlightLeg,
  HotelStay,
  TourSummary,
  TourTag,
} from "@family-memories/planner-schema";
