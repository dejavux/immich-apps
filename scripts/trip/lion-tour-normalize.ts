/**
 * @deprecated Import from @family-memories/planner/adapters/lion/normalize instead.
 * Thin re-export for scripts/trip CLI backward compatibility.
 */
export {
  buildShortName,
  compareTours,
  finalizeTour,
  formatTourCard,
  inferDestination,
  inferTags,
  toMmDd,
} from "@family-memories/planner/adapters/lion/normalize";
