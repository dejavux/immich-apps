/**
 * @deprecated Import from @family-memories/planner/adapters/lion/search instead.
 * Thin re-export for scripts/trip CLI backward compatibility.
 */
export {
  addTourDays,
  buildSearchBody,
  detailUrl,
  searchLionGroupList,
  searchLionGroupListAll,
  type LionNormGroupHit,
  type LionSearchParams,
  type LionSearchResult,
} from "@family-memories/planner/adapters/lion/search";
