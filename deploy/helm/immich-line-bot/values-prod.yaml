# Production overrides for immich-line-bot

image:
  pullPolicy: Always

env:
  logLevel: info
  # Passkey unlock grant persistence (immich-redis ClusterIP in immich namespace)
  redisUrl: "redis://immich-redis:6379"
