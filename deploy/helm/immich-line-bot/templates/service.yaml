apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich-line-bot.fullname" . }}
  labels:
    {{- include "immich-line-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immich-line-bot.selectorLabels" . | nindent 4 }}
