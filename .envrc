#!/usr/bin/env bash
# .envrc — direnv（可提交）；秘密在 .env（勿提交）
# lint-fix-agent 透過 direnv export 載入 CURSOR_API_KEY

watch_file .env

if [[ ! -f .env ]]; then
  echo "direnv: 警告 — 找不到 .env（請 cp .env.example .env 並填入 CURSOR_API_KEY）" >&2
else
  dotenv_if_exists .env
fi

PATH_add "./node_modules/.bin"
